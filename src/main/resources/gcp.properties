# GCP runtime configuration defaults
# All values can be overridden by:
#   1. Java system property:  -Dplugin_dir=/path/to/plugins
#   2. Environment variable:  PLUGIN_DIR=/path/to/plugins
#   3. Programmatic override: GCPConfig.load().with("plugin_dir", value)

# Directory scanned for ext_builder_*.jar plugin files (relative to working directory)
plugin_dir=ext_builders
